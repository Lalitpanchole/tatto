# 09 - Bug Tracker

## Active Bugs / Pending Client Requests
- **Merchandise Checkout**: Stripe Payment Link not yet integrated. Products using dummy printful URLs. (Pending — client to provide Stripe links)
- **Admin User Management Panel**: Add admin user management (Add Admin, Remove Admin, Edit Access Rights) directly in `AdminDashboard.jsx` under `TEAM` and `SETTINGS` tabs. (Pending implementation — STEP 3)
- **Official Domain Email Server Setup (@tattooplatz.ch)**: Client reported `@tattooplatz.ch` domain emails stopped receiving customer emails post-launch. Requires domain DNS MX record configuration by domain host/admin (Non-code DNS issue). (Reported by client — 2026-08-06)

## Resolved Bugs & Client Requests
- **Swiss (nFADP) & GDPR Cookie Consent Banner**: Implemented bottom-anchored responsive Cookie Consent Banner (`CookieBanner.jsx`) complying with Swiss Federal Data Protection Act (nFADP) and GDPR guidelines. Supports "Accept All", "Essential Only", and granular category preferences (Essential, Analytics, Marketing). Saves preferences in `localStorage` (`tattooplatz_cookie_consent`) for 365 days. Added "Cookie Preferences" link in `Footer.jsx` to re-open the preferences modal anytime on Local & Live sites. (Fixed — 2026-08-11 | 18:52 IST)
- **Admin Password Security & Password Change System (STEP 2 — COMPLETE)**: Fixed hardcoded Chris profile in `AdminDashboard.jsx` header (now dynamically displays logged-in user name & email like Bea, Lucy, etc.). Created production-grade Email 6-digit OTP Reset System ("Forgot Password?" modal on `LoginPage.jsx` -> Request OTP -> Verify 6-Digit Code -> Set New Password) and added "Change Admin Password" card in `AdminDashboard.jsx` Settings tab. Password updates save to MySQL `users` table via bcrypt hashing. (Fixed — 2026-08-06 | 14:58 IST)
- **Multiple Admin Team Member Logins (STEP 1 — COMPLETE)**: All 5 studio team members (`bea@tattooplatz.ch`, `lucy@tattooplatz.ch`, `tuli@tattooplatz.ch`, `dani@tattooplatz.ch`, `leonie@tattooplatz.ch`) can now log into Admin Portal. Created `seed-admins.js` script, added `createAdmin` + `updatePasswordByEmail` + `deleteById` to `userModel.js`, updated `LoginPage.jsx` to validate role during login and support all team emails. Initial password: `TattoPlatz@2026`. (Fixed — 2026-08-06 | 13:12 IST)

- **ESLint Configuration (Frontend)**: Fixed ESLint 8.57 flat config import error in `eslint.config.js` (`ERR_PACKAGE_PATH_NOT_EXPORTED`). (Fixed — 2026-08-05)
- **Backend Code Quality Scripts**: Added `eslint` devDependency + `eslint.config.js` + `npm run lint` script to Backend. (Fixed — 2026-08-05)
- **Studio Gallery Spacing (Client Request)**: Added `pt-16` top padding to Studio Gallery container in `About.jsx` (line 104). Client reported Studio Gallery was too close to the header above it. (Fixed — 2026-08-05 | 14:28 IST)
- **Pricing Card Description Text (Client Request)**: Removed all bullet/feature description text from 4 pricing session cards in `Pricing.jsx`. Removed `features` arrays from `plans` data and `<ul>` render block from JSX. (Fixed — 2026-08-05 | 14:41 IST)
- **Steps Section Text Alignment & Overlap (Client Request)**: Reduced giant STEP 1,2,3 font sizes to `lg:text-8xl`, adjusted black text spacing (`tracking-normal`, `leading-relaxed`), and expanded container spacing (`space-y-16`) in `Steps.jsx` for optimal readability and breathing room. (Fixed — 2026-08-05 | 15:03 IST)
- **VAT Removal (Client Request)**: Completely removed VAT (`VAT (Incl. 8.1%)`) display from cart drawer and checkout summary in `App.jsx`. Client confirmed business is not VAT registered. (Fixed — 2026-08-05 | 15:12 IST)
- **Cancellation Policy Text Update (Client Request)**: Updated missed appointment policy wording in `LegalPage.jsx`, `BookingTool.jsx`, and `ArtistDashboard.jsx` to *"If an appointment is missed without prior notice, we reserve the right to charge the full amount."*. (Fixed — 2026-08-05 | 15:15 IST)
- **Footer Contact Message Text Update (Client Request)**: Updated contact form confirmation text in `Footer.jsx` from *"We will get back to you within 24 hours."* to *"We'll get back to you as soon as possible."*. (Fixed — 2026-08-05 | 15:22 IST)
- **Booking Time Slot Overflow Bug (Mobile — CRITICAL)**: Fixed `BookingTool.jsx` — `Math.max(closingHour, openingHour + duration)` was incorrectly allowing 8-hour sessions to show slots like 11:00–19:00 or 12:00–20:00 that extend beyond closing time. Fixed in both `getAvailableSpotsForDay` and `getAvailableSlots` using strict `maxStartHour = closingHour - duration`. (Fixed — 2026-08-05 | 16:08 IST)
- **Persistent Login Bug (Client Request)**: Fixed `BookingTool.jsx` — `userProfile` now initializes from `localStorage` on mount. `handleFinalize()` skips Step 4 if already logged in. Step 4 shows "Logged in as X + Confirm Booking" card. "Not you? Sign out" link resets session. (Fixed — 2026-08-05 | 16:42 IST)
- **Static Invoice Number on Booking Receipt (Client UX Request)**: Replaced hardcoded `Invoice #TP-2026-09` in `BookingTool.jsx` with dynamic `invoiceNumber` (`Invoice #TP-2026-XXXXX`) generated per session timestamp. (Fixed — 2026-08-05 | 17:10 IST)
- **Artist Dashboard Calendar Month Navigation (Client Request)**: Added `<` (Previous Month) and `>` (Next Month) navigation controls to `ArtistDashboard.jsx` Reservations Calendar header so artists can browse through all months easily. (Fixed — 2026-08-05 | 17:26 IST)
- **Mobile vs Computer Artist Login Failure (CRITICAL)**: Integrated `authAPI.register` and `authAPI.login` with MySQL Backend in `BookingTool.jsx` with `.toLowerCase().trim()` email normalization. Accounts created on computer now persist globally to MySQL Database and can log in immediately on Mobile phones and other devices. (Fixed — 2026-08-05 | 17:41 IST)
- **False-Positive "Time slot already booked" Modal Error (CRITICAL)**: Updated `bookingController.js` with Smart Station Allocation fallback (autoselects next available Station 2, 3, or 4 if Station 1 is occupied) and added automatic live MySQL DB bookings sync in `App.jsx`. (Fixed — 2026-08-05 | 18:04 IST)

